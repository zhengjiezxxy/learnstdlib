#include <iostream>
#include <string>
#include <algorithm>
int a=5;
int b= 140;

void check(int n)
{
  int l = b-a;
  if(l<9)
    {
      if(x+b/10>0 && x+b/10<b+10-l)
	{
	  std::cout<< "errror" << std::endl;
	}
    }
}
  
int main()
{
  int i;
  std::vector<int> vec;

  while(std::cin >> i)
    {
      vec.push_back(i);
      for(size_t i=0;i<vec.size();++i)
	{
	  if(a>0&&b>0)
	    check(vec[i]);
	  else
	    break;
	  a /= 10;
	  b /= 10;
	}
      a = 5; b= 140;
    }
}
	  
